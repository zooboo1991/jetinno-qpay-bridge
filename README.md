# Jetinno ↔ QPay bridge

Jetinno JL30 кофены машиныг QPay-тэй холбох middleware. Машин болон QPay шууд харилцдаггүй — энэ сервер дунд нь орж орчуулна.

QPay клиент нь gmath.mn дээр ажиллаж байгаа холболтоос авсан: токен кэш, `payment_status` шалгалт, `cancelInvoice`-ийн `INVOICE_PAID` боловсруулалт бүгд тэндээс.

**Төлөв (2026-09-03):** бүтэн урсгал бодит JL30 машин дээр ажиллаж, жинхэнэ QPay төлбөрөөр кофе гарлаа.

## Машин суурилуулахад анхаарах гурван зүйл

Эдгээрийг мэдэхгүй бол суурилуулалт гацна — гурвуулаа бодит туршилтаар илэрсэн:

1. **WiFi холбосны дараа машиныг унтрааж асаа.** Сервертэй холбогдох процесс зөвхөн ачаалах үед эхэлдэг — WiFi-гүй асаасан машин дахин оролддоггүй тул порталд Offline хэвээр харагдана.
2. **QR Reverse Scan-г унтраа** (Machine Setting → Function Mode → Payment systems). Асаалттай бол машин уншигчаараа хэрэглэгчийн QR хайж, дэлгэцэндээ QR гаргахгүй. Бидэнд хэрэгтэй нь эсрэг горим — 正扫 (front-scan), өөрөөр хэлбэл `QR payment`.
3. **Кофены үнийг 0-ээс өөр болго** (Drink Setting). Үнэ 0 бол машин төлбөр асуухгүй.

Инженерийн цэс: дэлгэцийн зүүн дээд буланд удаан дарж, нууц үг `test9527`.

## Урсгал

1. Хэрэглэгч машины дэлгэцээс кофе сонгоно
2. Машин → `POST /jetinno/getQrCode` (энэ сервер рүү)
3. Сервер → QPay invoice үүсгэж, QR-ийг машинд буцаана
4. Хэрэглэгч банкны аппаараа дэлгэцийн QR-г уншуулж төлнө
5. QPay → `/qpay/callback/:orderNo`
6. Сервер QPay-аас **дахин баталгаажуулаад** машины `notifyUrl` руу `PAYSUCCESS` илгээнэ
7. Машин кофе гаргаад `POST /jetinno/productdone` илгээнэ

## Локал туршилт (QPay-гүйгээр)

```bash
cd ~/Downloads/jetinno-qpay-bridge && npm install
```

Бүтэн урсгалыг автоматаар шалгах:

```bash
npm run test:e2e
```

Энэ нь сервер + дуурайлган машин хоёрыг асааж, дараах 3 зүйлийг батална: кофе яг нэг удаа гарсан, давтагдсан `getQrCode` шинэ invoice үүсгээгүй, QPay callback `200 SUCCESS` буцаасан.

Гараар турших бол — нэг терминал дээр сервер:

```bash
PORT=3100 QPAY_MOCK=1 PUBLIC_URL=http://localhost:3100 npm start
```

Өөр терминал дээр машин:

```bash
BRIDGE_URL=http://localhost:3100 npm run simulate
```

Гарч ирсэн `curl .../mock/pay/TEST...` командыг ажиллуулбал төлбөр орсон мэт болж, "машин" кофе гаргана.

## Бодит QPay-тай холбох

```
QPAY_USERNAME=          # merchant.qpay.mn-аас
QPAY_PASSWORD=
QPAY_INVOICE_CODE=
# QPAY_BASE_URL=https://merchant.qpay.mn   # sandbox бол өөр

JETINNO_USERNAME=       # Jetinno-с (§2.1)
JETINNO_APIKEY=
PUBLIC_URL=https://таны-домэйн             # машин болон QPay хүрэх public https

# AMOUNT_DIVISOR=100    # доорх "нээлттэй асуудлууд"-ыг үзнэ үү
# ABANDON_AFTER_MS=600000
```

`QPAY_MOCK` тохируулахгүй орхино. `GET /health` нь `qpayConfigured` талбараар хувьсагчид бүрэн эсэхийг харуулна.

**Localhost дээр бодит QPay турших.** QPay `localhost` руу callback хүргэж чадахгүй. Туннел (ngrok, cloudflared) хэрэглэхгүй бол төлбөр төлсний дараа гараар:

```bash
curl http://localhost:3100/check/<orderNo>
```

Энэ нь callback-тай яг ижил `settle`-ийг дуудна — QPay-аас баталгаажуулаад машинд мэдэгдэнэ.

## Render дээр байршуулах

Vercel тохирохгүй: захиалгууд санах ойд байдаг тул serverless instance бүр өөр байх нь урсгалыг тасалдаг, `setInterval` sweep ажиллахгүй, Express-ийг дахин бичих хэрэгтэй болно. Байнга ажилладаг процесс хэрэгтэй.

**Free tier сонгож болохгүй.** 15 минут хүсэлтгүй бол унтарч, дараагийн хүсэлт дээр ~30 секунд сэрдэг. Jetinno-ийн хугацаа 8 секунд — шөнөжин зогссон машины өглөөний анхны үйлчлүүлэгч кофе авч чадахгүй гэсэн үг.

1. GitHub дээр repo үүсгэж push хийнэ (доор командууд бий)
2. Render → New → Web Service → тухайн repo-г сонгоно
3. Runtime `Node`, Region `Singapore`, Instance type `Starter`
4. Build `npm ci`, Start `npm start`, Health check path `/health`
5. Environment дээр `QPAY_USERNAME`, `QPAY_PASSWORD`, `QPAY_INVOICE_CODE`, `JETINNO_USERNAME`, `JETINNO_APIKEY` нэмнэ

`render.yaml` мөн байгаа (Blueprint хэлбэрээр), гэхдээ дашбоардаас гараар үүсгэх нь алдаа гаргах магадлал багатай.

`PUBLIC_URL` тохируулах шаардлагагүй — Render-ийн `RENDER_EXTERNAL_URL`-ээс автоматаар авна. Deploy хийсний дараа `https://<нэр>.onrender.com/health` дээр `qpayConfigured: true` гарч байвал бэлэн.

Jetinno-ийн маягтад бичих гурван хаяг:

```
https://<нэр>.onrender.com/jetinno/getQrCode      → qRUrl
https://<нэр>.onrender.com/jetinno/refund         → refundUrl
https://<нэр>.onrender.com/jetinno/productdone    → callbackUrl
```

## Олон эзэмшигч — Postgres (2-р үе шат)

Машин бүрийн мөнгө өөрийн эзэмшигчийн QPay данс руу орох ёстой тул эзэмшигч,
машин, захиалгыг Postgres (Supabase) дээр хадгална. **Одоогийн үе шатанд сан нь
хуулбар л барьж байгаа**: захиалга бүр санд бас бичигдэнэ, гэхдээ уншилт болон
"хоёр дахь кофе гаргах уу" гэх шийдвэрийг санах ой (`Map`) л гаргасаар байна.
Бичлэг нь `await`-гүй тул сан удаашрах, унах, огт байхгүй байсан ч кофе
алдагдахгүй. `DATABASE_URL` тавиагүй бол давхар бичлэг бүрэн унтарна.

Нэмэлт орчны хувьсагчид:

```
DATABASE_URL=            # Supabase pooler, порт 6543 (5432 нь зөвхөн IPv6,
                         # Render IPv4-тэй тул холбогдохгүй)
CRED_KEYS=k1:<base64>    # QPay нууц үг шифрлэх түлхүүр(үүд), `id:base64` хэлбэрээр
CRED_KEY_ACTIVE=k1       # шинэ шифрлэлтэд аль түлхүүрийг ашиглах
CRED_FP_KEY=<base64>     # мерчант давхардлыг илрүүлэх түлхүүрт HMAC
```

Түлхүүрүүдийг үүсгэх (гараар бичиж болохгүй — 32 санамсаргүй байт байх ёстой):

```bash
node -e "const c=require('crypto');console.log('CRED_KEYS=k1:'+c.randomBytes(32).toString('base64'));console.log('CRED_KEY_ACTIVE=k1');console.log('CRED_FP_KEY='+c.randomBytes(32).toString('base64'))"
```

Гарсан гурван мөрийг **Render-ийн Environment дээр болон локал `.env`-д адилхан**
тавина. Ялгаатай байвал өмнө шифрлэсэн нууц үгсийг задлах боломжгүй болно.

Эзэмшигч + машин бүртгэх:

```bash
node scripts/add-owner.js
```

Тестүүд (бүгд түр зуурын Docker Postgres дээр ажилладаг, Supabase-д хүрэхгүй):

```bash
npm run test:e2e        # машин → QR → төлбөр → кофе (сан шаардахгүй)
npm run test:crypto     # нууц үг битүүмжлэх/задлах
npm run test:store      # захиалгын төлөв, зэрэгцээ settle
npm run test:stats      # орлогын нэгтгэл, цагийн бүсийн зааг
npm run test:owner-api  # JWT шалгалт, эзэмшигчийн хамрах хүрээ, CORS
npm run test:sms        # webhook гарын үсэг, урилгын хаалга, зардлын хязгаар
```

QPay нууц үгийг гараар бичнэ — дэлгэцэнд харагдахгүй, логт үлдэхгүй, `ps`-ээр
харагдах `argv`-д ороогүй. Санд шифрлэгдсэн хэлбэрээр л хүрнэ. Иймд SQL editor-оор
эзэмшигч нэмж болохгүй: тэнд бичсэн нууц үг editor-ийн query history-д үлдэж,
dashboard-т нэвтрэх эрхтэй хүн бүрт уншигдана.

### Эзэмшигчийн API — `/owner/v1`

Портал (Next.js) браузераас **шууд** гүүр рүү хандана: Supabase-ийн access
token-ыг `Authorization: Bearer …` толгойд авчирна. Cookie ашиглахгүй тул CSRF
гэдэг гадаргуу энд огт байхгүй.

```
SUPABASE_URL=https://<project>.supabase.co   # JWT-г JWKS-ээр шалгахад
PORTAL_ORIGIN=https://kofe.mn                # зөвхөн энэ origin-д CORS нээнэ
```

`SUPABASE_URL` эсвэл `DATABASE_URL`-ийн аль нэг нь дутуу бол **роутер огт
mount хийгдэхгүй** — 404 буцаана. Нэг env мартагдсанаас болж хэн ч нэвтрэхгүй
роутер ажиллаж эхлэхээс 404 хамаагүй дээр. Асаалттай эсэхийг эхлэх логоос
харна: `owner api mounted` / `owner api NOT mounted`.

| Зам | Тайлбар |
|---|---|
| `GET /owner/v1/stats` | Хяналтын самбарын бүх тоо. `?ownerId=` нь зөвхөн уг хэрэглэгчийн удирддаг компанид зөвшөөрөгдөнө |
| `GET /owner/v1/me` | Хэрэглэгч ба удирддаг компаниуд нь |

Хамрах хүрээ (аль эзэмшигчийн өгөгдөл) нь **зөвхөн** шалгагдсан токеноос
гардаг — хүсэлтийн биеэс хэзээ ч биш. Гүүр Postgres-т `service_role`-оор
холбогддог тул бүх эзэмшигчийн мөрийг харах эрхтэй; хамрах хүрээг буруу
тогтоох нь нэг эзэмшигч нөгөөгийнхөө орлогыг харна гэсэн үг. `npm run
test:owner-api` энэ хилийг 23 тестээр шалгадаг — `alg:none`, нийтийн
түлхүүрээр HS256, өөр issuer, хугацаа дууссан токен, удирддаггүй компанийн
`ownerId` зэрэг.

### Нэвтрэх SMS — Supabase-ийн Send SMS hook

Supabase-ийн суурь SMS провайдерууд (Twilio, MessageBird, Vonage, Textlocal)
бүгд олон улсын A2P замаар Монгол руу очдог — үнэтэй, хүргэлт жигд бус. Тиймээс
дотоодын гарц ашиглана: Supabase кодоо энэ гүүр рүү илгээж, гүүр нь Skytel
Web2SMS рүү дамжуулна.

```
SEND_SMS_HOOK_SECRET=v1,whsec_...   # Supabase-ийн Hooks хуудаснаас
SMS_API_URL=http://web2sms.skytel.mn/apiSend
SMS_PARAM_TO=sendto
SMS_PARAM_TEXT=message
SMS_PARAM_KEY=token
SMS_API_KEY=<Web2SMS токен>        # одоогоор gmath-ийн данс
# SMS_API_METHOD өгөгдмөлөөр POST — доорх шалтгааныг үзнэ үү
```

**Skytel Web2SMS-д HTTPS байхгүй.** `web2sms.skytel.mn`-ийн 443 порт огт хариу
өгдөггүй (2026-09-05-нд шалгасан), тиймээс хүсэлт задгай сүлжээгээр явна.
Үүнийг тохиргоогоор засах арга алга. Хийж чадах зүйл нь **POST** ашиглаж
нэвтрэх код болон API токеныг URL-аас гаргах — тэгснээр замын дагуух хандалтын
логуудад тэдгээр орохгүй. Skytel-ийн endpoint POST-ын form биеийг query
string-тэй яг адилаар уншдаг нь бодит эцсийн цэг дээр шалгагдсан.

Үүнээс үлдэх эрсдэлийг **Supabase дээрх OTP-ийн хүчинтэй хугацааг богиносгож**
багасгана (өгөгдмөл 1 цаг — Authentication → Providers → Phone → OTP Expiry;
5 минут хангалттай). Барьж авсан код тэр хугацаанд л ашиглагдана.

Гарц 200 статустай хэрнээ бие дотроо алдаа буцаадаг (`{"status":0,...}`).
Кодод үүнийг **тохиргоо шаардахгүйгээр** барьдаг: JSON биеийн `status` нь 0
эсвэл `sent_count` нь 0 бол илгээлт амжилтгүйд тооцогдоно. Хоёр алдаа тэнцүү
биш — худал амжилт нь эзэмшигчийн цагийн квотыг иддэг, кодыг нь хүргэдэггүй,
бидэнд илгээсэн мэт харагддаг.

Supabase дээр: Authentication → Hooks → Send SMS → HTTP,
`https://<нэр>.onrender.com/hooks/send-sms`.

Гарц нь тохиргоогоор солигддог — Messagepro, Mobicom ижил хэлбэртэй тул
параметрийн нэрсээ солиход л хангалттай, дахин deploy хэрэггүй.

**Энэ бол интернэтээс ирсэн хүсэлт мөнгө зарцуулж, хүний утас дуугаргадаг цорын
ганц цэг.** Тиймээс дөрвөн хаалттай:

1. **Standard Webhooks гарын үсэг** — `webhook-id.webhook-timestamp.бие` дээрх
   HMAC-SHA256. Бие нь **түүхий байтаар** шалгагдана: задалж дахин цувруулбал
   түлхүүрийн дараалал өөрчлөгдөж гарын үсэг таарахаа болино.
2. **5 минутын цонх** — барьж авсан хүсэлтийг дахин илгээх боломжгүй.
3. **Урилгын хаалга** (`app.phone_may_receive_otp`) — гишүүн эсвэл нээлттэй
   урилгатай дугаар л код авна. Эс бөгөөс хэн ч дурын дугаар бичээд бидний
   дансаар SMS явуулна.
4. **Зардлын хязгаар** (`app.sms_budget`) — цагт 5, өдөрт 20. Зөвхөн амжилттай
   илгээлтийг тоолно: гарц унасны дараа сэргэхэд эзэмшигч цоожлогдох ёсгүй.

`public.sms_sends` нь юу илгээснийг бүртгэнэ, **юу бичсэнийг биш** — OTP ч,
мессежийн текст ч хадгалагдахгүй. Амьд код агуулсан лог бол түүнийг уншиж
чадах хүн бүрт нэвтрэх эрх өгсөн лог. Гарцын хариуг (`gateway_reply`) хадгална
— амжилтгүй илгээлтийг оношлоход зайлшгүй — гэхдээ кодыг эхлээд арчина.

**Данс хуваалцаж байгаагийн үр дагавар:** SMS gmath-ийн Web2SMS дансаар явна.
Илгээгчийн нэр тэднийх байх тул мессежийн эхэнд "Coffeine" гэж бичсэн. Мөн
үлдэгдэл дуусвал **хоёулаа зогсоно** — gmath-ийн масс илгээлт Coffeine-ий
нэвтрэлтийг зогсоож болно. `sms_sends` дэх амжилтгүй мөрүүд үүнийг харуулна.

Миграцууд `migrations/` дотор дугаарын дарааллаар. `000_supabase_stub_local_only.sql`
нь **зөвхөн локал Docker-т** — Supabase дээр байдаг зүйлийг дуурайдаг тул түүн дээр
битгий ажиллуул.

## Endpoint-ууд

| Зам | Хэн дуудна | Тайлбар |
|---|---|---|
| `POST /jetinno/getQrCode` | Машин | Invoice үүсгэж QR буцаана |
| `POST /jetinno/productdone` | Машин | Кофе гарсан эсэх |
| `POST /jetinno/refund` | Машин | Одоогоор `ERROR` буцаана — QPay буцаалт хараахан холбоогүй |
| `ALL /qpay/callback/:orderNo` | QPay | Үргэлж `200 SUCCESS` |
| `GET /check/:orderNo` | Та | Гараар баталгаажуулах |
| `GET /orders/:orderNo` | Та | Захиалгын төлөв харах |
| `GET /health` | Та | Тохиргоо бүрэн эсэх |

## Яагаад ийм бичсэн бэ

**`settle` дотор `settling` тугийг `await`-аас өмнө синхроноор тавьдаг.** QPay-ийн webhook болон `/check` хоёр яг нэг агшинд ирдэг нь бодит зүйл. Туггүй бол хоёул төлөв шалгалтыг давж, хоёул машинд мэдэгдэж, **нэг төлбөрт хоёр кофе** гарна. `npm run test:e2e` үүнийг 5 зэрэгцээ дуудлагаар шалгадаг — зөвхөн нэг нь 200 авах ёстой.

**Машин кофе гаргасныг баталсны дараа л `paid` болгодог.** Мэдэгдэл амжилтгүй бол захиалга дахин оролдох боломжтой хэвээр үлдэнэ — эс бөгөөс мөнгө авчихаад кофе өгөхгүй.

**Давтагдсан `getQrCode` хадгалсан QR-аа буцаана.** Машин 8 секундэд хариу аваагүй бол дахин асууна. QPay нэг `sender_invoice_no`-г хоёр дахь удаа хүлээж авахгүй тул шинэ invoice үүсгэж болохгүй.

**QPay callback үргэлж `200 SUCCESS`.** JSON эсвэл алдааны код буцаавал QPay давтаад байна.

**Төлөгдөөгүй QR-ыг 10 минутын дараа цуцалдаг.** Эс бөгөөс хэрэглэгч орхиод явсан QR сар хагасын дараа төлөгдөж, машин аль хэдийн мартсан захиалганд кофе гаргахгүй. Цуцлахад QPay `INVOICE_PAID` гэвэл тэр хүн үнэндээ төлсөн тул цуцлахын оронд гүйцээнэ.

## Нээлттэй асуудлууд

- **QR 128 тэмдэгтийн хязгаар — 2026-08-12-нд бодит утсаар батлагдсан.** QPay-ийн `qr_text` (банкны апп уншдаг жинхэнэ төлбөрийн QR) нь **242 тэмдэгт** — Jetinno-ийн 128-д багтахгүй. Харин `qPay_shortUrl` (27-28 тэмдэгт) багтдаг ч **банкны аппын QR уншигч татгалздаг** (QP2016 "QR код олдсонгүй") — учир нь энэ вэб холбоос, төлбөрийн код биш. Гарц: (а) Jetinno-с хязгаарыг ≥256 болгохыг хүсэх — үндсэн зам; (б) богино URL хэвээр үлдээж хэрэглэгчид **камераар** уншуулах — 2026-08-12-нд бодит төлбөрөөр батлагдсан: камер → хөтөч → QPay хуудас → банк сонгох → төлбөр гүйцсэн, мөнгө мерчант данс орсон (1% шимтгэл). Машин дээр "Утасны камераар уншуулна уу" заавар тавих шаардлагатай.
- **`orderAmount` нэгж.** Jetinno цент-ээр (100000 = 1000₮) илгээнэ гэж үзээд 100-д хуваасан. Бодит машины эхний `getQrCode` логийг хараад `AMOUNT_DIVISOR`-ыг тохируулна.
- **Хариу мессежийн гарын үсгийн талбарууд.** Баримт бичигт хүсэлтийн жишээ л бий, хариунд аль талбарууд орохыг заагаагүй. `src/sign.js` дахь `getQrCodeResponse` жагсаалтыг машинтай тулгаж баталгаажуулах хэрэгтэй.
- **`nonce`.** Баримтын §2.4 дурддаг ч ямар ч параметрийн хүснэгтэд байхгүй. `verifySign` нь ирсэн бол ашиглаж, эс бөгөөс алгасдаг — машины эхний хүсэлт үнэнийг хэлнэ.
- **Refund.** `/jetinno/refund` одоогоор `ERROR` буцаана. QPay-ийн буцаалттай холбох нь дараагийн алхам.
- **Олон эзэмшигч.** Схем, шифрлэлт, бүртгэлийн CLI бэлэн (дээрх хэсгийг үз) ба захиалга санд хуулбарлагдаж эхэлсэн. Үлдсэн ажил: уншилтыг санд шилжүүлэх, `deviceNo`-оор эзэмшигчийн QPay түлхүүрийг сонгож invoice үүсгэх, дараа нь орчны нэг мерчантын тохиргоог хасах.
- **Санах ойн хадгалалт.** Захиалгууд `Map` дотор бөгөөд шийдвэрийг мөн `Map` гаргасаар байна. Сервер дахин асахад төлөгдөөгүй захиалгууд санах ойгоос алга болно (санд үлдэнэ). Уншилтыг Postgres руу шилжүүлэх нь 3-р үе шат — түүнээс өмнө санд бичих хугацаа Jetinno-гийн 8 секундэд багтаж байгааг бодит ачааллын логоор баталгаажуулна.
